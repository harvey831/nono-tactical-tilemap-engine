# 📥 incoming — 原樣投遞區

澄羽那份既有題庫（`study_radiographer`）**原封不動**丟進這個資料夾就好，不用先轉格式。

NONO 會在雲端把它正規化成 `exam-bank/data/bank/{年}-{場次}-{科}.jsonl`，
跑 `validate_bank.py` 與 `coverage_report.py` 驗過之後再入庫。

## 為什麼不直接轉好再推

格式轉換的規則歸 NONO 管（schema 在 `../../schema/`），由同一邊做轉換與驗證，
才不會兩邊各轉一半、對不起來。你只要負責把原始資料原樣送到雲端讀得到的地方。

## 注意

* 保留澄羽原本的目錄結構與檔名，不要重新命名或合併
* 如果附帶她自己的盤點報告／答案檔／README，一併放進來，那些是校對用的
* 這個資料夾**不在** `.gitignore` 裡（`data/raw/` 才是），所以放進來的東西會進版控——
  這是刻意的，因為考試試題依著作權法第 9 條不受著作權保護，可以公開
* 但**大叔的個人作答紀錄絕對不要**放這裡，那個歸 `../../progress/`（已 gitignore）

正規化完成後，這裡的原始檔會保留當作對照來源，不刪。
